
# Camel JBang HTTP server port (controls Vert.x platform-http server port)
camel.jbang.platform-http.port=9999


# Managed by Camel Route Builder Studio

# Decoupled URIs for Chapter Samples
wiretap.audit.uri=stub:jms:queue:audit.trail

kafka.orders.uri=stub:kafka:topic:orders

ibmmq.request.uri=stub:jms:queue:REQUEST.Q

mongodb.orders.uri=stub:mongodb:cameldb?operation=insert

dlq.uri=stub:jms:queue:DLQ

ibmmq.queue=stub:IBMMQ.INBOUND.Q

# Remote target container host API URL for Deploy & Run Remotely
API_URL=http://localhost:8080

# Kafka Orders endpoint
kafka.orders.endpoint=stub:kafka:topic:orders

# IBM MQ default endpoint
ibmmq-endpoint=stub:jms:queue:IBMMQ.Q

# IBM MQ Request queue
ibmmq.request.queue=stub:jms:queue:REQUEST.Q

# IBM MQ Reply queue
ibmmq.reply.queue=stub:jms:queue:REPLY.Q

# IBM MQ Dead Letter Queue
ibmmq.dlq.queue=stub:jms:queue:DLQ

# IBM MQ XA Request queue
ibmmq.xa.request=stub:jms:queue:XA.REQUEST

# IBM MQ Audit Trail queue
ibmmq.audit.trail=stub:jms:queue:audit.trail

# Email notification queue
notify.email.queue=stub:jms:queue:NOTIFY.EMAIL

# SMS notification queue
notify.sms.queue=stub:jms:queue:NOTIFY.SMS

# MongoDB orders endpoint
mongodb.orders.endpoint=stub:mongodb:cameldb?operation=insert

# MongoDB transaction audit endpoint
mongodb.audit.transactions=stub:mongodb:myDb?consumerType=changeStream&database=audit&collection=transactions

# HTTP Orders endpoint
http.orders.endpoint=stub:http:api.example.com/orders

# HTTP Downstream API endpoint
http.downstream.endpoint=stub:http:downstream-service/api

# Customer Service direct stub
enricher.customer.service=stub:direct:fetch-customer

# Camel JBang HTTP server port (controls Vert.x platform-http server port)
camel.jbang.platform-http.port=9999

ibmmq-sink=stub:ibmmq-sink

ibmmq-source=stub:ibmmq-source

